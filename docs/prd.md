# Genora PRD

## 项目概述

Genora 是一个无限画布式 AI 创作工作台，目标是在同一个画布中完成图像、视频、文本和 Agent 辅助创作。用户可以通过节点连接组织创作流程，并将上游节点内容作为下游生成提示词或素材来源。

## 已确认功能

- 无限画布：支持节点添加、拖拽、连接、缩放、网格和小地图入口。
- 节点类型：
  - 图像：文生图，当前目标模型为 Agnes Image 2.1 Flash。
  - 视频：文本生视频，以及图片 + 提示词生视频，目标模型为 Agnes Video V2.0。
  - 文本：用于提示词、文案和创意生成。
  - 图片/视频素材：用户上传的本地参考素材。
- 节点交互：
  - 从节点连接点拖出后，可以弹出添加节点菜单，并自动连接新节点。
  - 节点下方提示词面板在悬停或选中时出现。
  - 支持按 Delete / Backspace 删除选中节点。
  - 生成结果应直接展示在对应节点内。
- Agent：
  - 右下角以圆形入口打开 Genora Agent。
  - Agent 可以读取当前画布摘要、节点连接关系、对话历史和对话框中上传的图片/视频附件信息。
  - Agent 输入区上传的图片/视频仅作为对话附件预览，不直接添加到画布。
  - 小灯泡按钮用于生成或切换不同 prompt 灵感。
- 视觉风格：
  - 默认深色模式，整体为黑色背景 + 液态玻璃风格。
  - Genora 设置中提供深色模式、浅色模式、主题色和字体大小调整。

## API 与模型约定

- Agnes API Key 只允许放在服务端环境变量 `AGNES_API_KEY` 中。
- 图片生成：
  - 模型：`agnes-image-2.1-flash`。
  - 当前接口只向 Agnes 发送模型名和 prompt，避免传入不支持的 OpenAI 风格参数。
- 视频生成：
  - 模型：`agnes-video-v2.0`。
  - 默认帧率：24 FPS。
  - 总帧数按 Agnes Video V2.0 要求对齐为 `8n + 1`。
  - 总帧数上限：441 帧。
- `docs/api/` 中的 KIE/GPT Image 2 文档仅作为第三方 API 参考，不等同于当前 Agnes 接口。

## 待确认问题

- 是否正式从本地 SQLite/Prisma 迁移到 Supabase。
- Supabase 项目的表结构、RLS 策略和 Storage Bucket 命名。
- 是否部署到 Vercel、Supabase Edge Functions 或其他平台。
- 是否需要接入登录后用户 ID，并替换当前临时用户名 `Genora`。
- 是否保留 KIE/GPT Image 2 API 作为备用图片模型或只使用 Agnes Image 2.1 Flash。

## 中文乱码修复规则

执行规则：
1. 先扫描项目中疑似乱码的内容，例如 �、涓、鍙、鏄、璇、æ、ç、å 等。
2. 只修复明显是中文乱码的文本。
3. 修复范围仅限：
   - 页面 UI 文案
   - toast / alert / error message
   - console 日志
   - 注释
   - README / md 文档
4. 禁止修改：
   - 变量名
   - 函数名
   - API 路径
   - 环境变量名
   - 模型名
   - 数据库字段
   - JSON key
   - package 依赖
5. 所有文件保存为 UTF-8。
6. 不要重构代码，不要优化逻辑。
7. 修复完成后输出修改文件清单和每处修复说明。

## 画布项目保存与作品库

- 画布项目使用本地 SQLite/Prisma 持久化，刷新浏览器或重启服务后仍可恢复。
- 保存内容包括画布节点、连线、视口位置与缩放比例。
- 本地上传的图片和视频需要先保存到服务端 `storage/uploads`，画布只保存可恢复的文件 URL。
- 支持 `Ctrl+S` / `Command+S` 立即保存当前项目。
- 当前项目每 10 分钟自动保存一次。
- 新项目默认名称为 `empty space`，创建后必须重新命名。
- 作品库使用独立页面展示，可新建、打开和重命名项目。
- 打开项目后回到无限画布，并恢复该项目最后保存的内容。
- 作品库不在页头显示“新建项目”按钮；项目网格第一项固定为加号新建卡片。
- 作品库项目卡和加号新建卡统一采用响应式 `16:9` 比例，对应 1920×1080 画面比例。

## 画布删除撤回

- 删除节点时同时记录该节点及其关联连线。
- 支持点击画布底部撤回按钮，或按 `Ctrl+Z` / `Command+Z`，恢复最近一次删除的节点及连线。
- 最多保留最近 20 次删除记录；撤回操作会将画布标记为待保存。

## 视频参考图交互

- 视频节点支持上传首帧和尾帧参考图。
- 已上传的参考图以缩略图展示，缩略图右上角提供删除按钮。
- 删除参考图后同步清除对应图片地址和文件名，并将画布标记为待保存。
- 无论当前已上传零张、一张或两张参考图，参考图区域右侧始终保留“添加图片”按钮。
- 首次添加填充首帧；首帧存在时再次添加填充或替换尾帧。
- 添加参考图按钮使用与缩略图一致的方形按钮，仅显示加号，不显示文字。
- 节点提示词面板以对应节点的水平中心线居中展开。

## 视频任务同步超时

- 视频任务同步超过 `AGNES_VIDEO_MAX_PROCESSING_SECONDS` 后，本地任务更新为 `failed`。
- 超时任务的 `error` 保存为 `Video task timeout`，`errorCode` 对外返回 `TIMEOUT`。
- 超时落库时保留最后一次 `lastRemoteStatus`，并将 `canResume` 更新为 `false`。
- `failed`、`completed`、`cancelled` 等终态任务不再进入后台视频同步扫描。
- 前端对 `failed + TIMEOUT` 状态显示“已超时”。

## 节点右键、打组与框选

- 右键节点显示复制、剪切、粘贴、打组、取消打组和删除操作。
- 支持 `Ctrl/Cmd+C`、`Ctrl/Cmd+X`、`Ctrl/Cmd+V`。
- 复制组时同时复制组内成员和成员之间的内部连线。
- 至少选择两个顶层普通节点后可创建真实容器组；拖动组会同步移动成员。
- 取消打组后保留成员当前绝对位置和已有连线；首期不支持嵌套组。
- 从组连接到新节点时，展开为每个组成员到目标节点的独立连线。
- 空白画布左键拖动用于框选，节点与框选区域部分相交即可选中。
- 鼠标中键按住拖动用于平移画布；左键拖动节点仍用于移动节点或整组。
- 鼠标悬停节点或框选节点时不打开提示词面板。
- 移除节点右下角提示词按钮，左键点击生成节点主体任意非控件区域时打开提示词面板。
- 点击一个生成节点时关闭其他节点的提示词面板；点击空白画布时关闭当前提示词面板。
- 多选框使用与节点一致的深色玻璃质感，并保留顶部打组按钮和左右连接加号。
